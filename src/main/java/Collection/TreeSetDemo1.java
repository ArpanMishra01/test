package Collection;import java.util.Comparator;import java.util.TreeSet;public class TreeSetDemo1 {    public static void main(String[] args) {        TreeSet t = new TreeSet(); // Default Natural Sorting Order compareTo() method of Comparable class is called        t.add(20);        t.add(0);        t.add(5);        t.add(10);        t.add(20);        System.out.println(t);        TreeSet t1 = new TreeSet(new MyComparator());  // for customized sorting        t1.add(20);        t1.add(0);        t1.add(5);        t1.add(10);        t1.add(20);        System.out.println(t1);    }}class MyComparator implements Comparator{    @Override    public int compare(Object o1, Object o2) {        Integer integer1 = (Integer) o1;        Integer integer2 = (Integer) o2;        // Too lengthy method//        if(integer1 > integer2)//            return -1;//        else if (integer1 < integer2)//            return +1;//        else//            return 0;        // using Default compareTo() method of Comparable class        //return - integer1.compareTo(integer2);    // or        return integer2.compareTo(integer1);//        return +1;  // to return same as insertion order//        return -1;  // to return reverse of insertion order//        return 0;   // only first element    }}