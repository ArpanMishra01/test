package ArrayProblems;import java.util.Collection;import java.util.Iterator;import java.util.SortedMap;import java.util.TreeMap;public class StringSorting {    public static void main(String[] args) {        String output = "";        String s = "i work in paytm";        String[] ar = s.split(" ");        SortedMap<Integer, String> hset = new TreeMap<Integer, String>();        //MultiValuedMap <Integer, String> hset = new MultiValuedMap<Integer, String>();        for (int i = 0; i < ar.length; i++) {            hset.put(ar[i].length(), ar[i]);            System.out.println(hset);        }        Collection c  = hset.values();        Iterator i = c.iterator();        while (i.hasNext()) {            output = output.concat(String.valueOf(i.next()) + " ");        }        System.out.println(output);    }}