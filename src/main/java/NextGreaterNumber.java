import java.util.Arrays;public class NextGreaterNumber {    public static void main(String[] args) {        int num = 12354;        System.out.println(nexGreaterInt(num));    }    public static int nexGreaterInt(int num){        char[] array = Integer.toString(num).toCharArray();        for (int i = array.length-2; i >=0; i++) {            if(array[i]< array[0]){                char temp = array[i];                array[i] = array[0];                array[0] = temp;                String s= (array.toString());                //char[] arr = Arrays.sort(s.substring(i+1).toCharArray());//                Integer result = Integer.parseInt(s.substring(0,i).concat(arr.toString()));//                return result;            }        }        return -1;    }}